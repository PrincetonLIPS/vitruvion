from setuptools import setup, find_packages

setup(
    name='sketchgraphs',
    version='0.1',
    packages=find_packages(include=['img2cad', 'sketchgraphs', 'sketchgraphs_models']),
    license='MIT'
)
